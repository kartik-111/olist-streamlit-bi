# Olist Streamlit BI Dashboard

A Python-first BI dashboard built in Streamlit using the Olist e-commerce dataset.

## How to run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run dashboards/app.py